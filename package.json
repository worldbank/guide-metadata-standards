{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:pdf": "npm run docs:build && press-export-pdf export docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vitepress-export-pdf": "^1.0.0"
  },
  "overrides": {
    "esbuild": "0.25.0"
  },
  "dependencies": {
    "markdown-it": "^14.1.0",
    "markdown-it-footnote": "^4.0.0"
  }
}

